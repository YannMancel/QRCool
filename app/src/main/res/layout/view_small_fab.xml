<?xml version="1.0" encoding="utf-8"?>

<!-- ConstraintLayout -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    android:id="@+id/view_root_small"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- TextView: Label -->
    <TextView
        android:id="@+id/view_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/margin_normal"
        app:layout_constraintTop_toTopOf="@id/view_small_fab"
        app:layout_constraintBottom_toBottomOf="@id/view_small_fab"
        app:layout_constraintEnd_toStartOf="@id/view_small_fab"
        android:alpha="0.0"
        tools:alpha="1.0"
        tools:text="Label"/>

    <!-- FloatingActionButton -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/view_small_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:fabSize="mini"
        tools:src="@drawable/ic_qrcode"
        style="@style/FloatingActionButtonStyle"/>

</androidx.constraintlayout.widget.ConstraintLayout>